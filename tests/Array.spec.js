import { expect } from 'chai';

describe('dummyTest', () => {
    it('should check array if values equal to ', () => { 

        const names = ['yarden','matan','shiraz'];
        expect(names.sort()).to.be.eql(['matan','shiraz','yarden']); }); });

